# 02 Active Projects

Purpose: current product, client, research, or internal tool work.

Each active project should have:

- `README.md` for purpose and setup
- local agent instructions if root rules are not enough
- `STATUS.md` or equivalent current-state notes
- links back to the command-center registry

Local project docs override broad workspace docs inside their scope.
